# Admin Panel

Bu papka `itcenter` asosiy loyihasidan alohida ishlaydi.

Ishga tushirish:

```powershell
cd admin-panel
npm install
npm start
```

Build:

```powershell
cd admin-panel
npm run build
```
